<!-- app/views/users/new.html.erb -->

<h2>Cadastro</h2>

<%= form_for :user, url: '/users' do |f| %>
  <%= f.label "Nome" %> <%= f.text_field :name %>
  <%= f.label "Email" %> <%= f.text_field :email, class: "validate" %>
  <%= f.label "Password" %> <%= f.password_field :password %>
  <%= f.label "Confirmação de senha" %> <%= f.password_field :password_confirmation %>

  <%= f.submit "Cadastrar", class: "btn waves-effect waves-light btn-large" %>

<% end %>
